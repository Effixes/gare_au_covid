<div class="main-wrapper waiting-wrapper text-center"
     id="<%= dom_id(@game) %>"
     data-reflex-root="[board]"
     data-controller="player"
     data-player-id-value="<%= @current_player.id %>">

  <%= render 'waiting_room', current_player: @current_player, game: @game %>
</div>
